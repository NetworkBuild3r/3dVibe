# Default capsule: API-critical queues. IncrementalScanJob / ScheduledScanJob
# run on VIBE_SCAN_QUEUE (default: scan) via a Sidekiq capsule with
# VIBE_SCAN_CONCURRENCY (default: 1). See config/initializers/sidekiq.rb.
:concurrency: 5
:queues:
  - default
  - print
  - search
  - previews
  - covers
  - curation
  - duplicates
